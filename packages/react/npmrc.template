# Template for packages/react/.npmrc — copy or rename to ".npmrc".
# The publish workflow does this automatically (see
# .github/workflows/publish-package.yml). Tracked as ".template" so
# the working file (which can carry a real token) stays out of git.
@tate2301:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
