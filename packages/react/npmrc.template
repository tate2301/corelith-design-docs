# Template for packages/react/.npmrc — copy or rename to ".npmrc" only when
# publishing locally. CI configures this through actions/setup-node.
# Tracked as ".template" so a real token stays out of git.
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
