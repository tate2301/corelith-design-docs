// Design tokens exposed as values, for the cases CSS variables can't cover —
// enumerating a palette in a picker, hashing a name onto a hue, inlining a
// colour into an SVG attribute.

export * from './accents';
